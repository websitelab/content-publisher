name: Auto-publish Blog Posts (24hr)

on:
  schedule:
    - cron: '0 * * * *'   # Check every hour
  workflow_dispatch:

jobs:
  auto-publish:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - run: npm ci

      - name: Auto-merge stale blog PRs
        run: node scripts/auto-publish.js
        env:
          GITHUB_PAT: ${{ secrets.ORG_GITHUB_PAT }}
